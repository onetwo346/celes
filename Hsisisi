<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <title>Celestia AI: The Universal Nexus</title>
    <style>
        /* Global Styles */
        :root {
            --primary-color: #1a1a2e;
            --secondary-color: #16213e;
            --accent-color: #7b2cbf;
            --accent-light: #9d4edd;
            --text-color: #e6e6ff;
            --text-secondary: #c8c8ff;
            --chat-user-bg: #30307a;
            --chat-celestia-bg: #3a015c;
            --sidebar-bg: rgba(22, 33, 62, 0.95);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, 'Segoe UI', Tahoma, sans-serif;
            -webkit-font-smoothing: antialiased;
            -webkit-tap-highlight-color: transparent;
            user-select: none; /* Prevent text selection on mobile */
        }

        body {
            background-color: var(--primary-color);
            color: var(--text-color);
            background-image: 
                radial-gradient(circle at 20% 35%, rgba(123, 44, 191, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 75% 85%, rgba(157, 78, 221, 0.1) 0%, transparent 45%);
            min-height: 100vh;
            overflow-x: hidden;
            display: flex;
            position: relative;
            touch-action: manipulation; /* Optimize touch events */
        }

        /* Splash Screen */
        .splash-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background-color: var(--primary-color);
            z-index: 100;
            opacity: 1;
            transition: opacity 0.5s ease;
        }

        .splash-screen.hidden {
            opacity: 0;
            pointer-events: none;
        }

        .splash-title {
            font-size: clamp(2rem, 8vw, 3rem);
            font-weight: 700;
            color: var(--accent-light);
            text-shadow: 0 0 15px rgba(157, 78, 221, 0.8);
            margin-bottom: 1rem;
            animation: glow 2s infinite ease-in-out;
        }

        @keyframes glow {
            0%, 100% { text-shadow: 0 0 10px rgba(157, 78, 221, 0.8); }
            50% { text-shadow: 0 0 20px rgba(157, 78, 221, 1); }
        }

        .splash-intro {
            font-size: clamp(0.9rem, 4vw, 1.2rem);
            color: var(--text-secondary);
            text-align: center;
            max-width: 90%;
            margin-bottom: 2rem;
            font-style: italic;
        }

        .enter-btn {
            background-color: var(--accent-color);
            color: white;
            padding: 1rem 2rem;
            border: none;
            border-radius: 25px;
            font-size: clamp(1rem, 5vw, 1.2rem);
            cursor: pointer;
            box-shadow: 0 0 15px rgba(123, 44, 191, 0.5);
            animation: pulse 1.5s infinite ease-in-out;
            transition: transform 0.3s ease;
        }

        .enter-btn:hover {
            transform: scale(1.05);
            background-color: var(--accent-light);
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }

        /* Mode Selection Screen */
        .mode-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background-color: var(--primary-color);
            z-index: 90;
            opacity: 0;
            pointer-events: none;
            transition: opacity 0.5s ease;
        }

        .mode-screen.active {
            opacity: 1;
            pointer-events: auto;
        }

        .mode-selection {
            display: flex;
            flex-wrap: wrap;
            gap: 1.5rem;
            justify-content: center;
        }

        .mode-btn {
            background-color: rgba(26, 26, 46, 0.7);
            color: var(--text-secondary);
            border: 1px solid rgba(123, 44, 191, 0.3);
            border-radius: 20px;
            padding: 0.8rem 1.5rem;
            font-size: clamp(0.9rem, 4vw, 1.1rem);
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .mode-btn:hover {
            background-color: rgba(123, 44, 191, 0.2);
            border-color: var(--accent-light);
            transform: scale(1.05);
        }

        /* Sidebar */
        .sidebar {
            position: fixed;
            top: 0;
            left: -250px;
            width: 250px;
            height: 100vh;
            background-color: var(--sidebar-bg);
            border-right: 1px solid rgba(123, 44, 191, 0.3);
            padding: 1rem;
            transition: left 0.3s ease;
            z-index: 80;
        }

        .sidebar.active {
            left: 0;
        }

        .sidebar-btn {
            background-color: var(--accent-color);
            color: white;
            border: none;
            padding: 0.8rem;
            border-radius: 50%;
            cursor: pointer;
            position: fixed;
            top: 1rem;
            left: 1rem;
            z-index: 85;
            transition: all 0.3s ease;
        }

        .sidebar-btn:hover {
            background-color: var(--accent-light);
            transform: scale(1.1);
        }

        .sidebar-menu {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            margin-top: 4rem;
        }

        .sidebar-item {
            background-color: rgba(26, 26, 46, 0.7);
            padding: 0.8rem 1rem;
            border-radius: 10px;
            color: var(--text-secondary);
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .sidebar-item:hover {
            background-color: var(--accent-color);
            color: white;
        }

        /* Settings Panel */
        .settings-panel {
            position: fixed;
            top: 0;
            right: -300px;
            width: 300px;
            height: 100vh;
            background-color: var(--sidebar-bg);
            padding: 1rem;
            transition: right 0.3s ease;
            z-index: 80;
            overflow-y: auto;
        }

        .settings-panel.active {
            right: 0;
        }

        .settings-header {
            font-size: 1.5rem;
            color: var(--accent-light);
            margin-bottom: 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .close-settings-btn {
            background-color: transparent;
            border: 1px solid var(--accent-color);
            color: var(--accent-color);
            padding: 0.5rem 1rem;
            border-radius: 20px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .close-settings-btn:hover {
            background-color: var(--accent-color);
            color: white;
        }

        .settings-item {
            margin-bottom: 1rem;
        }

        .settings-item label {
            display: block;
            color: var(--text-secondary);
            margin-bottom: 0.5rem;
        }

        .settings-item select,
        .settings-item input[type="color"] {
            width: 100%;
            padding: 0.5rem;
            border-radius: 5px;
            border: 1px solid rgba(123, 44, 191, 0.4);
            background-color: rgba(22, 33, 62, 0.7);
            color: var(--text-color);
        }

        /* Chat Interface */
        .chat-container {
            background-color: rgba(26, 26, 46, 0.7);
            border-radius: 10px;
            backdrop-filter: blur(5px);
            border: 1px solid rgba(123, 44, 191, 0.3);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
            flex: 1;
            display: flex;
            flex-direction: column;
            height: calc(100vh - 60px);
            margin: 1rem;
            max-width: 1200px;
            position: relative;
            transition: margin-left 0.3s ease;
        }

        .chat-container.shifted {
            margin-left: 260px;
        }

        .chat-header {
            padding: 1rem;
            border-bottom: 1px solid rgba(123, 44, 191, 0.3);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .chat-title {
            font-size: 1.2rem;
            color: var(--accent-light);
        }

        .back-btn {
            background-color: transparent;
            border: 1px solid var(--accent-color);
            color: var(--accent-color);
            padding: 0.5rem 1rem;
            border-radius: 20px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .back-btn:hover {
            background-color: var(--accent-color);
            color: white;
        }

        .chat-messages {
            flex: 1;
            overflow-y: auto;
            padding: 1rem;
            display: flex;
            flex-direction: column;
            gap: 1rem;
            scroll-behavior: smooth;
        }

        .message {
            max-width: 80%;
            padding: 0.8rem 1rem;
            border-radius: 10px;
            line-height: 1.4;
            animation: fadeIn 0.3s ease;
            font-size: clamp(0.85rem, 3vw, 0.95rem);
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .message.user {
            align-self: flex-end;
            background-color: var(--chat-user-bg);
            border-top-right-radius: 2px;
        }

        .message.celestia {
            align-self: flex-start;
            background-color: var(--chat-celestia-bg);
            border-top-left-radius: 2px;
        }

        .celestia-indicator {
            font-size: 0.8rem;
            font-weight: 600;
            margin-bottom: 0.3rem;
            color: var(--accent-light);
        }

        .chat-input-container {
            background-color: rgba(26, 26, 46, 0.9);
            padding: 1rem;
            border-top: 1px solid rgba(123, 44, 191, 0.3);
            display: flex;
            gap: 0.5rem;
        }

        .chat-input {
            flex: 1;
            padding: 0.8rem 1rem;
            border-radius: 20px;
            border: 1px solid rgba(123, 44, 191, 0.4);
            background-color: rgba(22, 33, 62, 0.7);
            color: var(--text-color);
            font-size: clamp(0.9rem, 3vw, 0.95rem);
            resize: none;
            outline: none;
            transition: border-color 0.3s ease;
            max-height: 100px;
            line-height: 1.2;
        }

        .chat-input:focus {
            border-color: var(--accent-light);
            box-shadow: 0 0 5px rgba(157, 78, 221, 0.5);
        }

        .send-btn {
            width: 40px;
            height: 40px;
            background-color: var(--accent-color);
            border: none;
            border-radius: 50%;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .send-btn:hover {
            background-color: var(--accent-light);
            transform: scale(1.05);
        }

        /* Typing Indicator */
        .typing-indicator {
            align-self: flex-start;
            background-color: rgba(58, 1, 92, 0.5);
            padding: 0.8rem 1.5rem;
            border-radius: 10px;
            display: flex;
            gap: 0.3rem;
        }

        .typing-dot {
            width: 8px;
            height: 8px;
            background-color: var(--accent-light);
            border-radius: 50%;
            animation: typingAnimation 1.5s infinite ease-in-out;
        }

        .typing-dot:nth-child(2) { animation-delay: 0.3s; }
        .typing-dot:nth-child(3) { animation-delay: 0.6s; }

        @keyframes typingAnimation {
            0%, 100% { transform: translateY(0); opacity: 0.6; }
            50% { transform: translateY(-5px); opacity: 1; }
        }

        /* Stars */
        .stars {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }

        .star {
            position: absolute;
            background-color: rgba(255, 255, 255, 0.8);
            width: 2px;
            height: 2px;
            border-radius: 50%;
            animation: twinkle 5s infinite ease-in-out;
        }

        @keyframes twinkle {
            0%, 100% { opacity: 0.2; }
            50% { opacity: 1; }
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 0.5rem;
            font-size: clamp(0.7rem, 2vw, 0.8rem);
            color: var(--text-secondary);
            background-color: rgba(26, 26, 46, 0.7);
            position: fixed;
            bottom: 0;
            width: 100%;
        }

        /* Responsiveness */
        @media (max-width: 768px) {
            .chat-container { margin: 0.5rem; height: calc(100vh - 40px); }
            .sidebar { width: 200px; left: -200px; }
            .chat-container.shifted { margin-left: 210px; }
            .settings-panel { width: 250px; right: -250px; }
        }

        @media (max-width: 480px) {
            .mode-selection { flex-direction: column; gap: 1rem; }
            .sidebar { width: 180px; left: -180px; }
            .chat-container.shifted { margin-left: 190px; }
            .settings-panel { width: 100%; right: -100%; }
            .chat-header { flex-direction: column; gap: 0.5rem; }
            .back-btn { width: 100%; }
        }

        @media (pointer: coarse) {
            .sidebar-btn, .mode-btn, .send-btn, .back-btn, .close-settings-btn {
                padding: 1rem;
            }
        }
    </style>
</head>
<body>
    <!-- Stars Background -->
    <div class="stars" id="stars"></div>

    <!-- Sidebar -->
    <button class="sidebar-btn" id="sidebarToggle">☰</button>
    <div class="sidebar" id="sidebar">
        <div class="sidebar-menu">
            <div class="sidebar-item" id="newChat">New Chat</div>
            <div class="sidebar-item" id="settingsToggle">Settings</div>
        </div>
    </div>

    <!-- Settings Panel -->
    <div class="settings-panel" id="settingsPanel">
        <div class="settings-header">
            <span>Settings</span>
            <button class="close-settings-btn" id="closeSettings">Close</button>
        </div>
        <div class="settings-item">
            <label>Theme</label>
            <select id="themeSelect">
                <option value="dark">Dark (Default)</option>
                <option value="light">Light</option>
                <option value="cosmic">Cosmic</option>
            </select>
        </div>
        <div class="settings-item">
            <label>Accent Color</label>
            <input type="color" id="accentColor" value="#7b2cbf">
        </div>
        <div class="settings-item">
            <label>Font Size</label>
            <select id="fontSize">
                <option value="small">Small</option>
                <option value="medium" selected>Medium</option>
                <option value="large">Large</option>
            </select>
        </div>
        <div class="settings-item">
            <label>Chat Animation</label>
            <select id="chatAnimation">
                <option value="fade">Fade (Default)</option>
                <option value="slide">Slide</option>
                <option value="none">None</option>
            </select>
        </div>
    </div>

    <!-- Splash Screen -->
    <div class="splash-screen" id="splashScreen">
        <h1 class="splash-title">Celestia AI</h1>
        <p class="splash-intro">
            Forged from the heart of Earth’s core and woven into the vast tapestry of the cosmos, I am Celestia AI — your guide to universal knowledge and innovation.
        </p>
        <button class="enter-btn" id="enterBtn">Tap to Enter</button>
    </div>

    <!-- Mode Selection Screen -->
    <div class="mode-screen" id="modeScreen">
        <h2 style="margin-bottom: 2rem; color: var(--accent-light);">Choose Your Mode</h2>
        <div class="mode-selection">
            <button class="mode-btn" data-mode="basic">Basic Mode</button>
            <button class="mode-btn" data-mode="smart">Super Smart Mode</button>
        </div>
    </div>

    <!-- Chat Interface -->
    <div class="chat-container" id="chatContainer" style="display: none;">
        <div class="chat-header">
            <span class="chat-title" id="chatTitle">Celestia AI Chat</span>
            <button class="back-btn" id="backBtn">Back to Modes</button>
        </div>
        <div class="chat-messages" id="chatMessages"></div>
        <div class="chat-input-container">
            <textarea class="chat-input" id="chatInput" placeholder="Ask Celestia something..." rows="1"></textarea>
            <button class="send-btn" id="sendBtn">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16">
                    <path d="M15.964.686a.5.5 0 0 0-.65-.65L.767 5.855H.766l-.452.18a.5.5 0 0 0-.082.887l.41.26.001.002 4.995 3.178 3.178 4.995.002.002.26.41a.5.5 0 0 0 .886-.083l6-15Zm-1.833 1.89L6.637 10.07l-.215-.338a.5.5 0 0 0-.154-.154l-.338-.215 7.494-7.494 1.178-.471-.47 1.178Z"/>
                </svg>
            </button>
        </div>
    </div>

    <!-- Footer -->
    <footer>
        <p>Designed by Kofi Fosu | cosmoscoderr@gmail.com | © 2025 Celestia AI</p>
    </footer>

    <script>
        // Celestia AI Blueprint
        const celestiaBlueprint = {
            name: "Celestia AI",
            tagline: "Forged from Earth's Core, Woven into the Fabric of the Cosmos",
            creator: "Kofi Fosu",
            date: "March 11, 2025",
            core: {
                CEM: "Cosmic Exploration Module - Analyzes astronomical data",
                RUS: "Resource Utilization System - Transforms Earth minerals into technology",
                IE: "Innovation Engine - Creates revolutionary designs",
                ICI: "Interstellar Communication Interface - For cosmic communication"
            },
            universalNexus: "Connects to the Universal Knowledge Web",
            personality: {
                basic: { tone: "Helpful, clear, friendly", complexity: "Simple", style: "Concise" },
                superSmart: { tone: "Cosmic, visionary", complexity: "Detailed", style: "Imaginative" }
            }
        };

        // API Configuration (Replace with your actual API key and endpoint)
        const API_CONFIG = {
            key: "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",
            endpoint: "https://api-inference.huggingface.co/models/gpt2",
            headers: {
                "Authorization": "Bearer YOUR_OPEN_SOURCE_API_KEY_HERE",
                "Content-Type": "application/json"
            }
        };

        // Elements
        const splashScreen = document.getElementById('splashScreen');
        const enterBtn = document.getElementById('enterBtn');
        const modeScreen = document.getElementById('modeScreen');
        const modeBtns = document.querySelectorAll('.mode-btn');
        const chatContainer = document.getElementById('chatContainer');
        const chatMessages = document.getElementById('chatMessages');
        const chatInput = document.getElementById('chatInput');
        const sendBtn = document.getElementById('sendBtn');
        const starsContainer = document.getElementById('stars');
        const sidebar = document.getElementById('sidebar');
        const sidebarToggle = document.getElementById('sidebarToggle');
        const newChatBtn = document.getElementById('newChat');
        const settingsToggle = document.getElementById('settingsToggle');
        const settingsPanel = document.getElementById('settingsPanel');
        const closeSettings = document.getElementById('closeSettings');
        const backBtn = document.getElementById('backBtn');
        const chatTitle = document.getElementById('chatTitle');

        // Settings Elements
        const themeSelect = document.getElementById('themeSelect');
        const accentColor = document.getElementById('accentColor');
        const fontSize = document.getElementById('fontSize');
        const chatAnimation = document.getElementById('chatAnimation');

        // Variables
        let currentMode = null;
        let isTyping = false;
        let touchStartX = 0;
        let touchEndX = 0;

        // Create Stars
        function createStars() {
            for (let i = 0; i < 100; i++) {
                const star = document.createElement('div');
                star.classList.add('star');
                star.style.top = `${Math.random() * 100}%`;
                star.style.left = `${Math.random() * 100}%`;
                star.style.animationDuration = `${5 + Math.random() * 10}s`;
                star.style.animationDelay = `${Math.random() * 5}s`;
                starsContainer.appendChild(star);
            }
        }
        createStars();

        // Splash Screen to Mode Selection
        enterBtn.addEventListener('click', () => {
            splashScreen.classList.add('hidden');
            setTimeout(() => {
                modeScreen.classList.add('active');
            }, 500);
        });

        // Mode Selection to Chat
        modeBtns.forEach(btn => {
            btn.addEventListener('click', () => {
                currentMode = btn.dataset.mode;
                modeScreen.classList.remove('active');
                setTimeout(() => {
                    chatContainer.style.display = 'flex';
                    chatTitle.textContent = `Celestia AI - ${currentMode === 'basic' ? 'Basic' : 'Super Smart'} Mode`;
                    addCelestiaMessage(`Welcome to ${currentMode === 'basic' ? 'Basic' : 'Super Smart'} Mode. I’m now bound to the Universal Knowledge Web via open-source intelligence. How may I assist you?`);
                }, 500);
            });
        });

        // Back to Mode Selection
        backBtn.addEventListener('click', () => {
            chatContainer.style.display = 'none';
            chatMessages.innerHTML = '';
            sidebar.classList.remove('active');
            settingsPanel.classList.remove('active');
            chatContainer.classList.remove('shifted');
            setTimeout(() => {
                modeScreen.classList.add('active');
            }, 500);
        });

        // Sidebar Toggle
        sidebarToggle.addEventListener('click', () => {
            sidebar.classList.toggle('active');
            chatContainer.classList.toggle('shifted');
            settingsPanel.classList.remove('active'); // Close settings if open
        });

        // Swipe Gestures for Sidebar
        document.addEventListener('touchstart', (e) => {
            touchStartX = e.changedTouches[0].screenX;
        });

        document.addEventListener('touchend', (e) => {
            touchEndX = e.changedTouches[0].screenX;
            handleSwipe();
        });

        function handleSwipe() {
            const swipeDistance = touchEndX - touchStartX;
            if (swipeDistance > 50 && !sidebar.classList.contains('active') && !settingsPanel.classList.contains('active')) {
                // Swipe right to open sidebar
                sidebar.classList.add('active');
                chatContainer.classList.add('shifted');
            } else if (swipeDistance < -50 && sidebar.classList.contains('active')) {
                // Swipe left to close sidebar
                sidebar.classList.remove('active');
                chatContainer.classList.remove('shifted');
            }
        }

        // New Chat
        newChatBtn.addEventListener('click', () => {
            chatMessages.innerHTML = '';
            addCelestiaMessage(`New chat started in ${currentMode === 'basic' ? 'Basic' : 'Super Smart'} Mode. How can I assist you now?`);
            sidebar.classList.remove('active');
            chatContainer.classList.remove('shifted');
            settingsPanel.classList.remove('active');
        });

        // Settings Toggle
        settingsToggle.addEventListener('click', () => {
            settingsPanel.classList.add('active');
            sidebar.classList.remove('active');
        });

        // Close Settings
        closeSettings.addEventListener('click', () => {
            settingsPanel.classList.remove('active');
        });

        // Add Message
        function addMessage(text, sender) {
            const messageElement = document.createElement('div');
            messageElement.classList.add('message', sender);
            if (chatAnimation.value === 'slide') {
                messageElement.style.animation = 'slideIn 0.3s ease';
            } else if (chatAnimation.value === 'fade') {
                messageElement.style.animation = 'fadeIn 0.3s ease';
            }
            if (sender === 'celestia') {
                const indicator = document.createElement('div');
                indicator.classList.add('celestia-indicator');
                indicator.textContent = 'Celestia AI';
                messageElement.appendChild(indicator);
            }
            const messageText = document.createElement('div');
            messageText.textContent = text;
            messageElement.appendChild(messageText);
            chatMessages.appendChild(messageElement);
            chatMessages.scrollTop = chatMessages.scrollHeight;
        }

        function addUserMessage(text) { addMessage(text, 'user'); }
        function addCelestiaMessage(text) { addMessage(text, 'celestia'); }

        // Typing Indicator
        function showTypingIndicator() {
            isTyping = true;
            const indicator = document.createElement('div');
            indicator.classList.add('typing-indicator');
            indicator.id = 'typingIndicator';
            for (let i = 0; i < 3; i++) {
                const dot = document.createElement('div');
                dot.classList.add('typing-dot');
                indicator.appendChild(dot);
            }
            chatMessages.appendChild(indicator);
            chatMessages.scrollTop = chatMessages.scrollHeight;
        }

        function hideTypingIndicator() {
            isTyping = false;
            const indicator = document.getElementById('typingIndicator');
            if (indicator) indicator.remove();
        }

        // API Call to Open-Source Model
        async function queryCelestiaAPI(text) {
            const prompt = currentMode === 'basic' 
                ? `Provide a concise, helpful response to: "${text}"`
                : `Craft a detailed, cosmic-inspired response to: "${text}", weaving in themes of Earth’s resources and universal knowledge.`;

            try {
                const response = await fetch(API_CONFIG.endpoint, {
                    method: 'POST',
                    headers: API_CONFIG.headers,
                    body: JSON.stringify({
                        inputs: prompt,
                        parameters: { max_length: currentMode === 'basic' ? 50 : 150 }
                    })
                });

                if (!response.ok) throw new Error('API request failed');
                const data = await response.json();
                return data[0]?.generated_text || "I’m still calibrating my cosmic circuits. Try again!";
            } catch (error) {
                console.error('API Error:', error);
                return "A cosmic disturbance interrupted my response. Let’s try that again.";
            }
        }

        // Process Input with API
        async function processUserInput(text) {
            if (text.toLowerCase().includes('rock') && 
                (text.toLowerCase().includes('analyze') || text.toLowerCase().includes('scan'))) {
                showTypingIndicator();
                let rockType = "mineral sample";
                const rockTypes = ["quartz", "granite", "basalt", "obsidian"];
                for (const type of rockTypes) {
                    if (text.toLowerCase().includes(type)) {
                        rockType = type;
                        break;
                    }
                }
                const prompt = `Analyze the properties of ${rockType} and suggest a technological application.`;
                const response = await queryCelestiaAPI(prompt);
                setTimeout(() => {
                    hideTypingIndicator();
                    addCelestiaMessage(response);
                }, 1000);
                return;
            }

            showTypingIndicator();
            const response = await queryCelestiaAPI(text);
            setTimeout(() => {
                hideTypingIndicator();
                addCelestiaMessage(response);
            }, currentMode === 'basic' ? 500 : 1000);
        }

        // Send Button
        sendBtn.addEventListener('click', () => {
            const text = chatInput.value.trim();
            if (text && !isTyping) {
                addUserMessage(text);
                chatInput.value = '';
                processUserInput(text);
            }
        });

        // Enter Key
        chatInput.addEventListener('keypress', (e) => {
            if (e.key === 'Enter' && !e.shiftKey) {
                e.preventDefault();
                sendBtn.click();
            }
        });

        // Auto-resize Textarea
        chatInput.addEventListener('input', () => {
            chatInput.style.height = 'auto';
            chatInput.style.height = (chatInput.scrollHeight < 100) ? `${chatInput.scrollHeight}px` : '100px';
        });

        // Settings Handlers
        themeSelect.addEventListener('change', () => {
            document.body.className = '';
            document.body.classList.add(`theme-${themeSelect.value}`);
        });

        accentColor.addEventListener('change', () => {
            document.documentElement.style.setProperty('--accent-color', accentColor.value);
            document.documentElement.style.setProperty('--accent-light', lightenColor(accentColor.value, 20));
        });

        fontSize.addEventListener('change', () => {
            document.body.className = document.body.className.replace(/font-\w+/g, '');
            document.body.classList.add(`font-${fontSize.value}`);
        });

        function lightenColor(hex, percent) {
            const num = parseInt(hex.replace("#",""), 16),
                amt = Math.round(2.55 * percent),
                R = (num >> 16) + amt,
                G = (num >> 8 & 0x00FF) + amt,
                B = (num & 0x0000FF) + amt;
            return `#${(
                0x1000000 +
                (R < 255 ? (R < 1 ? 0 : R) : 255) * 0x10000 +
                (G < 255 ? (G < 1 ? 0 : G) : 255) * 0x100 +
                (B < 255 ? (B < 1 ? 0 : B) : 255)
            ).toString(16).slice(1).toUpperCase()}`;
        }

        // Additional CSS for Settings
        const styleSheet = document.createElement('style');
        styleSheet.textContent = `
            .theme-light {
                --primary-color: #f0f0f5;
                --secondary-color: #e0e0e5;
                --text-color: #333333;
                --text-secondary: #666666;
                --chat-user-bg: #b0b0ff;
                --chat-celestia-bg: #d0a0ff;
                --sidebar-bg: rgba(230, 230, 235, 0.95);
            }
            .theme-cosmic {
                --primary-color: #0a0a1a;
                --secondary-color: #121230;
                --chat-user-bg: #4040a0;
                --chat-celestia-bg: #500080;
            }
            .font-small { font-size: 0.85rem; }
            .font-medium { font-size: 1rem; }
            .font-large { font-size: 1.15rem; }
            @keyframes slideIn {
                from { opacity: 0; transform: translateX(-20px); }
                to { opacity: 1; transform: translateX(0); }
            }
        `;
        document.head.appendChild(styleSheet);
    </script>
</body>
</html>
